name: Go tests

on: [push]

permissions:
  contents: write

jobs:
  lint:
    runs-on: ubuntu-latest
    environment: test-env
    steps:
      - name: Install Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.23
      - name: Check out repository
        uses: actions/checkout@v2
      #      - name: Go vet complexity
      #        uses: shoooooman/go-complexity-analysis-action@v1
      #        with:
      #          github_token: ${{ secrets.GITHUB_TOKEN }}
      #          cycloover: 10
      #          halsteadover: 50

      - name: Install golint and go vet
        run: |
          go install github.com/rauzh/go-complexity-analysis/cmd/complexity@9a7cad4
          go install github.com/golangci/golangci-lint/cmd/golangci-lint@v1.62.2

      - name: Launch govet
        run: |
          go vet -vettool="$(which complexity)" --cycloover 10 --halsteadover 40 ./...
          if [ $? -ne 0 ]; then exit 1; fi

      - name: Launch golint
        run: |
          golangci-lint run domain/... internal/... pkg/...
          if [ $? -ne 0 ]; then exit 1; fi

  tests:
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.22.0'

      - name: Run unit tests
        run: make ci-unit -i

      - name: Run integration tests
        run: make ci-integration -i

      - name: Run E2E tests
        run: make ci-e2e -i

      - name: Processing report data
        run: make ci-concat-reports

      - name: Load test report history
        uses: actions/checkout@v3
        if: always()
        continue-on-error: true
        with:
          ref: gh-pages
          path: gh-pages

      - name: Build test report
        uses: simple-elf/allure-report-action@v1.7
        if: always()
        with:
          gh_pages: gh-pages
          allure_history: allure-history
          allure_results: allure-results

      - name: Publish test report
        uses: peaceiris/actions-gh-pages@v3
        if: always()
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: allure-history